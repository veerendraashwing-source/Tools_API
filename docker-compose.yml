services:
  easychit-api:
    image: nihargoel/easychit-api:latest
    container_name: easychit-api
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./appsettings.json:/app/appsettings.json:ro
